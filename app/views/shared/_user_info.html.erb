<%= link_to gavatar_for(current_user, size: 80), current_user %>
<h1><%= current_user.name %></h1>
<span><%= link_to t("user_info.view_my_profile"), current_user %></span>
<span><%= pluralize @micropost_quantity, t("user_info.micropost") %></span>
